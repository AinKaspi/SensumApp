import UIKit

class RankViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
        view.backgroundColor = .red // Или другой цвет фона для этой вкладки
        // Пока оставим пустым, скоро добавим UI
    }
}
