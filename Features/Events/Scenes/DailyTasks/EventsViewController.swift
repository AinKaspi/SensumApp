import UIKit

class EventsViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
        view.backgroundColor = .black // Или другой цвет фона для этой вкладки
        // Пока оставим пустым, скоро добавим UI
    }
}
